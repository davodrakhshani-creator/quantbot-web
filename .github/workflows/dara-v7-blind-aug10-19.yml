name: DARA v7 Blind Aug10-19
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'dara_v7_blind_aug10_19.py'
      - '.github/workflows/dara-v7-blind-aug10-19.yml'
permissions:
  contents: write
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run frozen DARA v7 blind test
        run: python dara_v7_blind_aug10_19.py
      - name: Persist result
        run: |
          git config user.name "quantbot-research-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/dara_v7_blind_aug10_19.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Add DARA v7 blind Aug10-19 result"
          git pull --rebase origin main
          git push origin HEAD:main
