name: QuantBot v4 T2 Audit

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'research_v4_t2_audit.py'
      - '.github/workflows/quantbot-v4-t2-audit.yml'

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --quiet pandas numpy requests
      - name: Run T2 audit
        run: python research_v4_t2_audit.py
      - name: Persist T2 audit state
        run: |
          git config user.name "quantbot-research-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/v4_t2_audit.json
          if git diff --cached --quiet; then
            echo "No state change"
            exit 0
          fi
          git commit -m "Record QuantBot v4 T2 adversarial audit"
          git pull --rebase origin main
          git push origin HEAD:main
