name: QuantBot v19 Japan Tick Clone

on:
  workflow_dispatch:
  push:
    paths:
      - 'research_v19_japan_tick_clone.py'
      - '.github/workflows/quantbot-v19-japan-tick-clone.yml'

permissions:
  contents: write

jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: pip install requests pandas numpy
      - name: Compile
        run: python -m py_compile research_v19_japan_tick_clone.py
      - name: Run v19 Japanese tick clone
        run: python research_v19_japan_tick_clone.py
      - name: Persist state
        if: always()
        run: |
          git config user.name "quantbot-research-bot"
          git config user.email "actions@users.noreply.github.com"
          if [ -f data/v19_japan_tick_clone_state.json ]; then
            git add data/v19_japan_tick_clone_state.json
            git diff --cached --quiet && exit 0
            git commit -m "Update QuantBot v19 Japan tick clone state"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
