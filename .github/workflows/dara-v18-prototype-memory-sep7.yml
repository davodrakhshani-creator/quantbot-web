name: DARA v18 Prototype Memory Sep7
on:
  push:
    paths: ['dara_v18_prototype_memory_sep7.py','.github/workflows/dara-v18-prototype-memory-sep7.yml']
  workflow_dispatch:
permissions: {contents: write}
jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Run DARA v18 Prototype Memory Sep7
        run: python dara_v18_prototype_memory_sep7.py
      - name: Persist result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/dara_v18_prototype_memory_sep7.json
          git diff --cached --quiet || git commit -m "Persist DARA v18 prototype-memory Sep7 result"
          git push
