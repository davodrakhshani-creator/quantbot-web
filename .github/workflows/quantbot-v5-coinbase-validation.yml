name: QuantBot v5 Independent Coinbase Validation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'research_v5_coinbase_validation.py'
      - '.github/workflows/quantbot-v5-coinbase-validation.yml'

permissions:
  contents: write

jobs:
  coinbase-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --quiet pandas numpy requests
      - name: Run frozen T2 on independent Coinbase spot data
        run: python research_v5_coinbase_validation.py
      - name: Persist validation state
        run: |
          git config user.name "quantbot-research-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/v5_coinbase_validation.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Update independent Coinbase T2 validation"
          git pull --rebase origin main
          git push origin HEAD:main
